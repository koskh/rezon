// @flow"
import React from 'react';

const LoginLayout = props => {
    return (
      <section>
        <header>
                login header
        </header>

        <main className="container">
          Login Form
        </main>

      </section>
    );
};

export default LoginLayout;
