import React from 'react';

const LoginLayout = props => {
    return (
      <section>
        <header>
                login header
        </header>

        <main className="container">
          Login Form
        </main>

        <footer>
            login footer
        </footer>

      </section>
    );
};

export default LoginLayout;
